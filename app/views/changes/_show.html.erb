<p class= "acc-changes"><%= @change.find_text %> -> <%= @change.replace_text %><%= link_to 'X', change_path(@change), :confirm => 'Are you sure you want to delete this change?', :class => 'delete', :method => :delete %></p>